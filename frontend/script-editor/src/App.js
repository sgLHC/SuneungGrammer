import React from "react";
import { MyPlugin } from "./MyPlugin"; // MyPlugin 컴포넌트를 가져옵니다.

const App = () => {
  return (
    <div>
      <MyPlugin />
    </div>
  );
};

export default App; // App 컴포넌트를 기본 내보내기로 설정합니다.
